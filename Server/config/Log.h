#pragma once

#include <QLoggingCategory>
#include <QString>

// Категория «жизнь сервера»: версия, порты, какой конфиг прочитан, что в нём
// не понято, сколько памяти отдано под окна. Эти строки печатаются даже на
// уровне errors — личного в них нет, а без них не поднявшийся контейнер
// молчал бы в `docker logs`, и человеку нечем себе помочь.
Q_DECLARE_LOGGING_CATEGORY(lcApp)

namespace Log {

// Что сервер пишет в журнал.
//
// Умолчание — Errors. У Ferry обещание «сервер не пишет на диск ничего»
// (§10) стоит в README, а stdout в докере оседает в `docker logs` навсегда.
// Идентификатор раздачи, IP и имя получателя — это ровно то, чего мы
// обещали не хранить, поэтому по умолчанию их нет.
enum class Level {
    Off,      // молчание, включая ошибки
    Errors,   // строки старта, предупреждения и ошибки  ← умолчание
    Normal,   // + события: создана раздача, подключился получатель, закрыта
};

Level levelFromString(const QString &s, bool *ok = nullptr);
QString levelToString(Level level);

// Ставит обработчик сообщений Qt. Зовётся из main один раз, сразу после
// чтения конфига — до того, как появятся первые события.
void install(Level level);

// Попадают ли в журнал идентификаторы раздач и адреса. Сервер отдаёт это в
// GET /api/health: обещание приватности должно быть проверяемым, а не
// подразумеваемым.
bool keepsIdentifiers();

} // namespace Log
