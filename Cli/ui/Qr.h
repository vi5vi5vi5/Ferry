#pragma once

#include <string>

namespace ferry::ui {

// Печатает QR со ссылкой. false — напечатать не вышло: цвет выключен
// (тогда квадрат нечем нарисовать так, чтобы он сканировался), вывод не в
// терминал, ссылка не влезла в разумную версию кода или терминал слишком
// узкий. Это не ошибка: ссылка и так напечатана рядом текстом.
bool printQr(const std::string &text);

} // namespace ferry::ui
