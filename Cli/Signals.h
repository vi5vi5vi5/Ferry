#pragma once

// Ctrl-C и SIGTERM.
//
// Обработчик только поднимает флаг — и это не перестраховка: из обработчика
// сигнала нельзя звать почти ничего, включая printf и работу с сокетом.
// Всё остальное делает главный цикл, когда до флага дойдёт: закрывает
// сокет по-человечески, возвращает курсор и не оставляет после себя
// недописанный файл.
namespace ferry::cli {

void installSignalHandlers();
bool stopRequested();

} // namespace ferry::cli
